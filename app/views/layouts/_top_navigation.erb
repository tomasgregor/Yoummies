<div class="top_nav_container">
	<ul>
		<li><%= link_to "Posts", posts_url %></li>
		<li><%= link_to "Foods", foods_url %></li>
		<li><%= link_to "Shops", shops_url %></li>
		<% if session[:user_id] %>
			<li>Hello <%= link_to "#{User.find_by_id(session[:user_id]).name}", user_url(session[:user_id]) %></li>
			<li><%= link_to "Sign Out", sign_out_url %></li>
		<% else  %>
			<li><%= link_to "Sign In", sign_in_url %> or <%= link_to "Sign Up", new_user_url %></li>
		<% end  %>
	</ul>
</div>

<!-- #{User.find_by_id(:user_id).name} -->